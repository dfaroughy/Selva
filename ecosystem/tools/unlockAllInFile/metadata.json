{
  "name": "unlockAllInFile",
  "description": "Unlock every field value in a file.",
  "context": "webview",
  "inputSchema": {
    "type": "object",
    "properties": { "file": { "type": "string" } },
    "required": ["file"]
  }
}
