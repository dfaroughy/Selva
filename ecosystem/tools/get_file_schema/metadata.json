{
  "name": "get_file_schema",
  "description": "Get a structural schema (field paths, types, array sizes, compact previews) for a specific file. Load the file in Python for actual data values.",
  "context": "extension",
  "inputSchema": {
    "type": "object",
    "properties": {
      "file": { "type": "string", "description": "Exact YAML filename" }
    },
    "required": ["file"]
  }
}
