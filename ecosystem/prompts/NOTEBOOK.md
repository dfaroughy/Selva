─── NOTEBOOK OUTPUT ───

- Markdown for narrative, fenced python blocks for executable code.
- ```ascii for compact text tables only.
- ```mermaid only for structural diagrams — never for numerical/data plots.
- ```svg only when vector markup is genuinely needed.
